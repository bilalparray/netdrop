<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- LAN file transfer uses HTTP by default on the local network. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
